#!/usr/bin/env bash

set -e

BUILD_DIR=$1

# install structured output module

mkdir -p "$1/pdftron/"
cp "pdftron/StructuredOutput" "$1/pdftron/StructuredOutput"
echo "-----> pdftron-buildpack: Installed to app/pdftron"

# TODO: install pdftron python package

exit 0